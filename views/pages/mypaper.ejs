
<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lobster&effect=shadow-multiple">
  <title>Template Playground</title>
<style>
h1 {
  font-size: 48pt;
  color: red;
  font-family: "Lobster", Sans-serif;
}
</style>
</head>
<body onLoad="nav('Home')">
<h1 style="font-family: 'Lobster', Sans-serif; text-align: center;">myHomeTownGazette</h1>
<button onClick="nav('Home')">Home</button>
<button onClick="nav('formDemo')">Form</button>
<button onClick="nav('ListDemo');ListDemo();">List</button>

<div id="results"></div>
<div id="lResults"></div>

<script id="formDemo" type="text/template">
<h2>Sample Form Entry</h2>
<form name="form1" id="form1" onsubmit="event.preventDefault();">
<input type="text" id="name" placeholder="Name" /><br /><br />
<input type="text" id="address" placeholder="Address" /><br /><br />
<input type="submit" onClick = "funct1()" value="Press" />
</script>

<script id="Home" type="text/template">
<h2>Sample Home Page</h2>
<h2>This might actually work like I want it to</h2>
<p>Now is the time for all men to come to the aid of their country</p>
</script>

<script id="Listing" type="text/template">
<h2>Sample Data Output Listing</h2>
<div id="lResults"></div>
</script>

<script type="text/javascript">

inzip = '68450';

base_url = `https://hometowngazette-931f6-default-rtdb.firebaseio.com/${inzip}.json`

function nav(page) {
  try {
    document.title = page;
    tmp = document.getElementById(page).innerHTML;
    document.getElementById("results").innerHTML = tmp;
  } catch {
    console.log('Oops');  
  }
}

function funct1() {
  name = document.getElementById("name").value;
  addr = document.getElementById("address").value;
  alert(base_url);
  alert(`Hello ${name} from ${addr} how you doing today`);
  document.getElementById("name").value = "";
  document.getElementById("address").value = "";
};


function ListDemo() {
  eHTML = "<h2 style='text-align: center;'>Sample Data Output Listing</h2>";
  for (i=1;i<25;i++){
    eHTML += `This is row ${i}, what do you think of it<br />`;
  }
  document.getElementById("results").innerHTML = eHTML;
}
/*
document.getElementById("formBtn").addEventListener("click", function(event){
  event.preventDefault();
  name = document.getElementById("name").value;
  addr = document.getElementById("address").value;
  alert(`Hello ${name} from ${addr} how you doing today`);
});
*/

/*
                full_name = fname + ' '+middle+' , '+lname;
                hOut += templateHTML
                    .replace(/{{record_key}}/g, record_key)
                    .replace(/{{full_name}}/g, full_name);
                hArr.push(hOut);


      function loadAll() {
        document.getElementById("demo").innerHTML = "";
        var xhttp = new XMLHttpRequest();
        xhttp.onreadystatechange = function () {
          if (this.readyState == 4 && this.status == 200) {
            document.getElementById("demo").innerHTML = "";
            snap = JSON.parse(this.responseText);
              refRes = document.getElementById("demo")
              refOut="";
              refOut = "<table style='width: 350px;' cellspacing='2' cellpadding='2' class='bordered striped'>";
              refOut += "<thead><tr><th>Delete</th><th>Edit</th><th>Name</th><th>Phone</th></tr></thead><tbody>"
            Object.keys(snap).forEach(key => {
              refOut += `<tr><td><i class="material-icons pointer" onclick="killIt('${key}')">delete</i></td><td><i class="material-icons pointer"
              onclick="loadOne('${key}')">mode_edit</i></td><td>${snap[key].name}</td><td>${snap[key].phone}</td></tr>`;
            })
            refOut += "</tbody></table>";
            refRes.innerHTML = refOut;
          }
         };
        xhttp.open("get",work_url, true);
        xhttp.setRequestHeader("Content-type", "application/json");
        xhttp.send();
      }

*/

</script>
</body>

</html>

